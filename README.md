# Craftar — site

Landing page do Craftar, um build system para harnesses de AI coding.

Site estático de um arquivo: `index.html` (sem build, sem dependências) e `llms.txt`.

Publicado com GitHub Pages a partir da branch `main` (raiz).
